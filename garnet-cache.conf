# Garnet - In-Memory Cluster Configuration
# Placeholders: $eth1 (resolved per-node), $port (resolved per-instance)

--bind $eth1
--port $port
--cluster-enabled true
--no-persistence
--index-max-size 1g
--log-level info
